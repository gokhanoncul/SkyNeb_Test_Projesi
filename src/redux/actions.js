import { createAction } from "@reduxjs/toolkit";


//actions 
export const orderBook = createAction("orderBook");
export const liveOrder = createAction("liveOrder");
export const liveTrade = createAction("liveTrade");

